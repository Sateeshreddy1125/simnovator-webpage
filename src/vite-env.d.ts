
/// <reference types="vite/client" />

// This declares the experimental React version to TypeScript
declare namespace React {
  interface ReactExperimentalMode {
    // Add any experimental flags here as they become available
  }
}
