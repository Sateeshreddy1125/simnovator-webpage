
import React from 'react'
import ReactDOM from 'react-dom'
import App from './App.tsx'
import './index.css'

// With the experimental version, we should use the legacy render method
ReactDOM.render(<App />, document.getElementById("root"));
